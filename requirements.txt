asgiref==3.9.2  # channels 4.3.2 needs >=3.9,<4
Django==5.2.17  # v3.19.11: PYSEC-2026-3717 / CVE-2026-15830 (GeoDjango WKT/WKB DoS — not reachable here, but it reddens CI and blocks every push)
icalendar==6.1.0
psycopg2-binary==2.9.10
python-dotenv==1.2.2
python-magic==0.4.27
pytz==2025.2
requests==2.33.1

# ⚠️ v3.19.10 — 0.5.5 → 0.6.0 CLOSES FOUR CVEs (CVE-2026-54284, -59893,
# -59894, -71491): three parser DoS/ReDoS paths and a quote-escaping bug in the
# Python/PHP output filters. sqlparse is a *transitive* dependency (Django
# requires >=0.3.1) and nothing in this codebase imports it — verified by grep
# across `src/` and `Parliament/` on 08-17-26 — so none of the four is reachable
# from a request. It is pinned here anyway because CI's pip-audit step has no
# `continue-on-error`, so a known CVE in a pinned version fails the build
# whether or not it is exploitable.
sqlparse==0.6.0
user-agents==2.2.0

# App server: Daphne (ASGI — HTTP + WebSockets). See daphne pin below with channels.

# Redis for shared caching across workers (optional but recommended)
redis==5.2.1
django-redis==7.0.0

# Celery — async task queue and periodic task scheduling
celery==5.4.0
django-celery-beat==2.9.0

# Web Push notifications (VAPID-signed push via pywebpush)
pywebpush==2.3.0

# Field-level encryption for sensitive data (AES encryption via Fernet)
#
# ⚠️ v3.19.10 — 48.0.1 → 50.0.0 CLOSES THREE ADVISORIES, AND ONE OF THEM IS
# REACHABLE FROM AN UNAUTHENTICATED-ADJACENT ENDPOINT.
#
#   PYSEC-2026-3553  exponential blowup resolving an invalid certificate chain
#                    that repeats a self-signed cert. **This is the one that
#                    matters here**: `verify_registration_response` in
#                    `src/view/webauthn.py` parses an attestation statement the
#                    *client* supplies, and an attestation statement carries an
#                    x5c certificate chain. A crafted chain is CPU on the
#                    passkey-registration path. Fixed in 49.0.0.
#   PYSEC-2026-3554  a name-constrained sub-CA can be escaped by a wildcard SAN
#                    on the leaf. Not reachable — Parliament verifies no server
#                    certificates itself. Fixed in 49.0.0.
#   PYSEC-2026-3552  Bleichenbacher oracle in `pkcs7_decrypt_*`. Not reachable —
#                    this codebase uses Fernet, never PKCS7. Fixed in 50.0.0.
#
# ⚠️ THE BUMP IS NOT SELF-CONTAINED: `pyOpenSSL` 26.2.0 pins
# `cryptography<49`, so pip will also pull pyOpenSSL forward (26.4.0 resolves
# cleanly). pyOpenSSL is a transitive dep of daphne/twisted and is deliberately
# left unpinned; if it is ever pinned, pin the pair together.
#
# Verified 08-17-26: full suite green (1,340 tests, 0 failures) on
# cryptography 50.0.0 + sqlparse 0.6.0 + pyOpenSSL 26.4.0, and pip-audit reports
# the requirements file clean.
cryptography==50.0.0

# Markdown rendering for changelog display
markdown==3.8.1

# HTML sanitization to prevent stored XSS from rich-text editors
bleach==6.4.0

# DOCX to HTML conversion for document preview
mammoth==1.11.0

# Image handling — DIRECT dependency (profile_picture, landing photos/og_image,
# security screenshot ImageFields all require Pillow), previously installed
# only by luck as reportlab's transitive dep. Pinned v3.16.1 after the
# preflight pip-audit flagged a stale installed copy.
pillow==12.3.0

# PDF generation for chapter minutes export
reportlab==5.0.0

# PDF to image conversion for mobile document viewing
PyMuPDF==1.27.2.3

# Excel file generation for directory export
openpyxl==3.1.2

# Two-Factor Authentication (TOTP)
django-otp==1.5.4
qrcode==7.4.2

# REST API (3.0.0 foundation)
djangorestframework==3.16.0

# WebSockets (3.0.0 — replaces HTTP polling for chat)
channels==4.3.2
channels-redis==4.2.0
daphne==4.2.2

# Passkeys / WebAuthn (3.0.0 — optional fast-path login bypassing password + 2FA)
webauthn==2.7.1

# ⚠️ v3.19.9 — REQUIRED FOR `manage.py test --parallel` TO REPORT A FAILURE AT ALL.
#
# Django's parallel runner moves results between processes by pickling them, and
# a failure's `sys.exc_info()` tuple contains a traceback object, which is not
# picklable. `django.test.runner` handles this by calling
# `tblib.pickling_support.install()` — IF tblib is importable. It was not, so
# `django.test.runner.tblib` was `None` and EVERY failure under `--parallel`
# died in the parent as:
#
#     TypeError: cannot pickle 'traceback' object
#
# with no `FAIL:` line, no module and no test name — and, worse, it ABORTS the
# run, so the other 1,312 results are never reported either. Django does print
# an explanatory block naming the test, but the child prints it after the parent
# has already raised, so across eight workers it is easy to miss entirely. That
# is exactly what happened on 08-13-26 (v3.19.8 §5, recorded there as
# unresolved and unreproducible).
#
# Reproduced and fixed-verified 08-15-26 in a minimal two-module project:
# without tblib, one failing assertion under `--parallel=2` produced the
# TypeError and no results; with tblib, the same tree reported
# `FAILED (failures=1)` and a complete traceback.
#
# Pinned as a hard dependency rather than a dev extra because `requirements.txt`
# is the only dependency file this project has, and a test tool that is only
# installed on the machines that remembered to install it is the failure mode
# `src/test_parallel_reporting.py` exists to prevent.
tblib==3.2.2
