# Parliament v2.7.1 - Announcement Email Improvements

**Release Date:** March 2, 2026
**Status:** Deployed
**Type:** Feature Enhancement & Bug Fixes

---

## Overview

This release enhances the announcement email system with detailed logging, scheduled email delivery, and UX improvements to the email confirmation flow.

---

## Key Features

### 1. Email Logs Console Output

Added detailed step-by-step console output to email logs, providing full visibility into what happens during email sends.

**Features:**
- Real-time logging of each step in the email send process
- User-by-user breakdown showing exactly who received emails
- Visibility expansion details (e.g., "Member" expanding to include Chair, Officer)
- Clear status counts: sent, skipped (inactive, visibility, no email, disabled)
- Error messages captured for failed sends
- Terminal-style dark display in admin interface

**Example Console Output:**
```
[14:32:15.123] ============================================================
[14:32:15.124] STARTING EMAIL SEND
[14:32:15.125] ============================================================
[14:32:15.126] Announcement: 'Chapter Meeting Reminder' (ID: 45)
[14:32:15.127] Initiated by: John Smith
[14:32:15.128]
[14:32:15.129] VISIBILITY SETTINGS:
[14:32:15.130]   Raw visible_to: ['Member']
[14:32:15.131]   Type: <class 'list'>
[14:32:15.132]
[14:32:15.133] VISIBILITY EXPANSION:
[14:32:15.134]   Original types: ['Member']
[14:32:15.135]   'Member' found - expanding to include Chair, Officer
[14:32:15.136]   Final targeted types: ['Member', 'Chair', 'Officer']
...
```

---

### 2. Scheduled Announcement Emails

Announcements with future `publish_at` dates can now have emails automatically sent when they become published.

**How It Works:**
1. Create announcement with future `publish_at` date
2. Check "Send Email Notifications" checkbox
3. System remembers preference with `send_email_on_publish=True`
4. Cron job runs every 5 minutes to check for due announcements
5. When `publish_at` passes, emails are sent automatically
6. `email_sent_at` timestamp recorded, flag cleared

**Management Command:**
```bash
# Run manually
python manage.py process_scheduled_announcements

# Dry run (shows what would be processed)
python manage.py process_scheduled_announcements --dry-run
```

**Cron Setup:**
```bash
# Add to crontab (every 5 minutes)
*/5 * * * * cd /path/to/Parliament && /path/to/venv/bin/python manage.py process_scheduled_announcements >> /var/log/parliament_scheduled.log 2>&1
```

---

### 3. Email Confirmation UX Improvements

Improved the email confirmation page to reduce accidental skips.

**Changes:**
- Send button now primary/prominent (green)
- Skip option moved to smaller text link
- Confirmation popup added when clicking Skip
- Clearer messaging about what each option does

---

## Bug Fixes

### System Logs Page Blank

**Issue:** The system logs page at `/admin/view-logs/` was completely blank.

**Cause:** Log parsing expected a different format than the actual log file structure. The code was splitting on ` - ` but logs used a different delimiter pattern.

**Fix:** Implemented proper regex-based log parsing:
```python
LOG_PATTERN = re.compile(r'^(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2},\d{3}) \[(\w+)\] ([^:]+): (.*)$')
```

### Email Logs Not Appearing

**Issue:** New announcements weren't appearing in the email logs list.

**Cause:** If any error occurred during the email send process, the entire function would fail before the log entry was saved.

**Fix:** Restructured `send_announcement_notification()` to:
1. Create the `AnnouncementEmailLog` record **first**
2. Wrap all processing in `try/finally` block
3. Always save the log record, even on errors
4. Capture error messages in `error_message` field

---

## Database Changes

### New Fields

**Announcement model:**
| Field | Type | Description |
|-------|------|-------------|
| `send_email_on_publish` | BooleanField | Flag to send emails when scheduled announcement publishes |
| `email_sent_at` | DateTimeField | Timestamp when emails were sent |

**AnnouncementEmailLog model:**
| Field | Type | Description |
|-------|------|-------------|
| `console_log` | TextField | Detailed debug log of the send process |

### Migration

```bash
python manage.py migrate src
```

Migrations:
- `0119_add_console_log_field.py` - Adds console_log to AnnouncementEmailLog
- `0120_add_announcement_email_scheduling.py` - Adds scheduling fields to Announcement

---

## File Changes

### Modified Files

| File | Changes |
|------|---------|
| `src/models.py` | Added `console_log` to AnnouncementEmailLog, `send_email_on_publish` and `email_sent_at` to Announcement |
| `src/notifications.py` | Major rewrite with console logging buffer, try/finally error handling, added `process_pending_scheduled_announcements()` |
| `src/admin.py` | Fixed `view_logs` with proper regex parsing |
| `src/view/officer/manage_announcements.py` | Updated create flow to set `send_email_on_publish` for scheduled announcements |
| `templates/officer/confirm_announcement_email.html` | Swapped buttons, added skip confirmation popup |
| `templates/admin_v2/email_log_detail.html` | Added console output section with terminal styling |

### New Files

| File | Purpose |
|------|---------|
| `src/management/commands/process_scheduled_announcements.py` | Management command for cron-based scheduled email processing |
| `src/migrations/0119_add_console_log_field.py` | Migration for console_log field |
| `src/migrations/0120_add_announcement_email_scheduling.py` | Migration for scheduling fields |

---

## Deployment Steps

1. **Pull latest code**
   ```bash
   git pull origin main
   ```

2. **Run migrations**
   ```bash
   python manage.py migrate src
   ```

3. **Set up cron job** (for scheduled announcements)
   ```bash
   crontab -e
   # Add:
   */5 * * * * cd /path/to/Parliament && /path/to/venv/bin/python manage.py process_scheduled_announcements >> /var/log/parliament_scheduled.log 2>&1
   ```

4. **Restart application**
   ```bash
   sudo systemctl restart gunicorn
   ```

---

## Testing Checklist

- [ ] Create immediate announcement with "Send Email" - verify emails sent and console log appears
- [ ] Create scheduled announcement with "Send Email" - verify `send_email_on_publish=True` is set
- [ ] Wait for scheduled time, run management command - verify emails sent
- [ ] Check email log detail page - verify console output displays correctly
- [ ] Visit `/admin/view-logs/` - verify logs display properly
- [ ] Test skip confirmation popup on email confirmation page

---

## Contributors

- [Mason Kimball](https://github.com/MasonKimball05) - Lead Developer

---

**Last Updated:** March 2, 2026
**Version:** 2.7.1
**Previous Version:** 2.7.0
