# v2.11.0 - Security Attack Mitigation & Admin Dashboard Redesign

**Release Date:** April 7, 2026

**Deployment Status:** Pending

**Type:** Security Enhancement & UI Redesign

---

## Overview

Major security update introducing a full suite of attack mitigation tools: auto-quarantine, honeypot/poison pill endpoints, emergency lockdown mode, and security email notifications. Also includes a complete redesign of the admin-v2 dashboard with a modern card-based layout using Alpine.js.

---

## Security Features

### Session Tracking Middleware

**Problem:** The preferences page showed 0 active sessions for all users.

**Root Cause:** Sessions were not being tracked on each request.

**Solution:** Created `SessionTrackingMiddleware` that updates session records on each authenticated request, throttled to 5-minute intervals using cache to reduce DB load.

**Files Changed:**
- `src/middleware/session_tracking.py` *(new)*
- `Parliament/settings_postgres.py` — added middleware to `MIDDLEWARE` list

---

### Auto-Quarantine System

Accounts that trigger attack detection thresholds (20+ attacks from a single IP) are automatically quarantined. Quarantined accounts cannot log in until manually released by an admin.

**How it works:**
1. Security middleware tracks attack count per IP
2. When threshold is crossed, the associated user account is quarantined
3. Login view checks `is_quarantined` flag before authenticating
4. Admin can release quarantine via admin-v2 or Django admin

**Files Changed:**
- `src/middleware/security.py` — added auto-quarantine trigger
- `src/view/login_view.py` — added quarantine check pre-login
- `src/models.py` — added `is_quarantined` field to `ParliamentUser`, added `QuarantinedAccount` model

---

### Honeypot / Poison Pill Endpoints

Fake admin URLs are now active. Any access to these endpoints triggers an immediate 24-hour IP ban and security alert email.

**Honeypot URLs:**
- `/wp-admin/`
- `/phpmyadmin/`
- `/.env`
- `/admin/backup/`
- `/api/v1/users/export/`

**Files Changed:**
- `src/view/honeypot.py` *(new)*
- `src/urls.py` — added honeypot URL patterns

---

### Emergency Lockdown Mode

One-click emergency lockdown blocks all logins except for IPs on the whitelist. A user-friendly lockdown page is shown to blocked visitors.

**Features:**
- Singleton `SystemLockdown` record in DB
- Whitelisted IPs (comma-separated) can still log in
- Custom message shown to blocked users
- Activation/deactivation logged with admin and timestamp

**Files Changed:**
- `src/middleware/lockdown.py` *(new)*
- `src/models.py` — added `SystemLockdown` model
- `templates/lockdown.html` *(new)*
- `Parliament/settings_postgres.py` — added `EmergencyLockdownMiddleware`

---

### Security Email Notifications

Critical security events now trigger alert emails to the configured `SECURITY_ALERT_EMAIL` address.

**Events that trigger alerts:**
| Event | Threshold |
|-------|-----------|
| Attack blocks | 10+ attacks from same IP |
| Failed logins | 5+ from same IP in 15 minutes |
| Account quarantined | Any auto-quarantine |
| Honeypot triggered | Any access |
| Lockdown activated | Immediately |
| Lockdown deactivated | Immediately |

**Files Changed:**
- `src/security_notifications.py` *(new)*
- `src/models.py` — added `SecurityNotificationLog` model
- `Parliament/settings_postgres.py` — added `SECURITY_ALERT_EMAIL` setting

---

## Admin Dashboard Redesign

The admin-v2 dashboard was completely rebuilt from ~977 lines to ~470 lines with a modern card-based layout.

### New Layout

- **Card-based design** with expandable/collapsible sections (Alpine.js `x-data`, `@click`, `x-show`)
- **Security card** prominently at top with attack stats, quarantine count, blocked IP count
- **Lockdown alert banner** shown when system is in lockdown
- **Quick stats grid** for at-a-glance numbers: users, logins, votes, upcoming events
- **Organized cards:** Security, Users, Content, Email & Notifications, System, Performance
- **Dark mode** supported throughout
- **Responsive** for desktop, tablet, mobile

### Template Changes
- `templates/admin_v2/dashboard.html` — complete rewrite

---

## New Admin-v2 Security Pages

| URL | Description |
|-----|-------------|
| `/admin-v2/security/` | Security dashboard — attack stats, recent events |
| `/admin-v2/security/quarantine/` | View and release quarantined accounts |
| `/admin-v2/security/lockdown/` | Activate/deactivate emergency lockdown |
| `/admin-v2/security/honeypot/` | Honeypot access log |
| `/admin-v2/security/notifications/` | Security notification history |

**Files Changed:**
- `src/view/admin_v2.py` — added 5 new views
- `src/urls.py` — added security management URL patterns
- `templates/admin_v2/security_dashboard.html` *(new)*
- `templates/admin_v2/quarantine_management.html` *(new)*
- `templates/admin_v2/lockdown_control.html` *(new)*
- `templates/admin_v2/honeypot_logs.html` *(new)*
- `templates/admin_v2/security_notifications.html` *(new)*

---

## Django Admin Additions

All new security models are now accessible via the Django admin portal:

| Model | Admin Features |
|-------|---------------|
| `IPWhitelist` | View/add/edit whitelisted IPs |
| `IPBlacklist` | View/add/edit blocked IPs, block count tracking |
| `QuarantinedAccount` | View quarantines, status badge, release tracking |
| `HoneypotAccess` | Read-only log of all honeypot hits |
| `SystemLockdown` | View/edit lockdown state (no delete) |
| `SecurityNotificationLog` | Read-only audit trail with severity badges |

---

## Database Changes

Migration: `0136_honeypotaccess_parliamentuser_is_quarantined_and_more.py`

| Change | Description |
|--------|-------------|
| `ParliamentUser.is_quarantined` | Boolean field, default False |
| `QuarantinedAccount` | New model |
| `HoneypotAccess` | New model |
| `SystemLockdown` | New model |
| `SecurityNotificationLog` | New model |

---

## Files Changed Summary

| File | Change |
|------|--------|
| `src/middleware/session_tracking.py` | Created |
| `src/middleware/lockdown.py` | Created |
| `src/security_notifications.py` | Created |
| `src/view/honeypot.py` | Created |
| `templates/lockdown.html` | Created |
| `templates/admin_v2/security_dashboard.html` | Created |
| `templates/admin_v2/quarantine_management.html` | Created |
| `templates/admin_v2/lockdown_control.html` | Created |
| `templates/admin_v2/honeypot_logs.html` | Created |
| `templates/admin_v2/security_notifications.html` | Created |
| `src/models.py` | Added 4 models + is_quarantined field |
| `src/admin.py` | Registered 6 new models |
| `src/middleware/security.py` | Added auto-quarantine + notifications |
| `src/view/admin_v2.py` | Added 5 security views + dashboard context |
| `src/view/login_view.py` | Added quarantine check |
| `src/urls.py` | Added honeypot + security management URLs |
| `Parliament/settings_postgres.py` | Added 2 middleware + SECURITY_ALERT_EMAIL |
| `templates/admin_v2/dashboard.html` | Complete redesign |

---

## Deployment Notes

1. **Run migrations:**
   ```bash
   python manage.py migrate
   ```

2. **Set environment variable:**
   ```bash
   SECURITY_ALERT_EMAIL=your-admin-email@example.com
   ```

3. **No data migration required** — all new models start empty.

4. **Test honeypots** by visiting `/wp-admin/` while logged in as admin to confirm logging works.

5. **Verify lockdown** by activating/deactivating from `/admin-v2/security/lockdown/` in a staging environment before deploying.
