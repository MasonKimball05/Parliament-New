# v3.25.2 — 08-24-26

**Committed & pushed:** 08-25-26, `13240de`
**Deployed:** *not deployed*
**Migration:** none. No model, no dependency, no seeder.

The 08-24 auto-run fix batch, reviewing v3.25.0 (`ea0dd36`) and v3.25.1
(`7296056`, committed while this review was running). Both hold up — every
finding here came from somewhere neither of them was looking.

**Written in two passes.** The second was requested and is not a formality: it
found that the first pass's headline fix was itself a guard written against the
instance, established the mechanism the first pass had recorded as unknown, and
closed all three items the first pass left open.

| | | |
|---|---|---|
| 🔴 | `/officers/system-logs/` named Kai reporters and parties to every officer | fixed at **four** source sites + at render, 31 tests |
| 🟠 | a failing `subTest` aborted the whole `--parallel` run, reporting nothing | fixed + 11 tests, mechanism established |
| 🟡 | the production security configuration was executed by zero tests | 7 tests |
| 🟡 | `/officers/system-logs/` had a wider audience than the pages it records | gate narrowed + 2 tests |
| 🟡 | `@log_function_call` on `ParliamentUserAdmin` turned the class into a function | fixed + 3 tests |

---

## 🔴 1. The twelfth Kai surface, and the first that is not a database row

`src/decorators.py::log_function_call` writes one line per call:

```
2026-08-24 10:54:12,370 [INFO] function_calls: User reporter_guy called
submit_kai_report with arguments: (), {}, Action: No specific action
```

Thirteen views in `src/view/kai_reports.py` carry that decorator. **On
`submit_kai_report` the caller *is* the reporter**, so that line is the single
fact `can_view_submitter_identity` exists to withhold, in plain text with the
member's username attached.

`/officers/system-logs/` tails the last 200 lines of `django_actions.log` and
renders them verbatim, behind a gate that consults **no `KaiMemberPermission`
anywhere**. Reproduced end to end 08-24-26 through the real endpoints: a member
loaded `/kai/submit-report/`; a plain Officer then loaded the log page and the
response body contained `reporter_guy` beside `submit_kai_report`.

> ⚠️ **v3.18.2'S RULE WAS "ENUMERATE THE MODELS THAT CAN STORE IT FIRST", AND A
> LOG FILE IS NOT A MODEL.** That release listed, correctly, the four places a
> confidential value comes to rest under a different name — *an audit
> description, a notification body, an email subject and a log line* — and then
> went looking through `apps.get_models()`, which can only ever find three of
> them. **The durable form: enumerate the things that STORE it, and a file is
> storage.**

### ⚠️ And the first fix for it was a guard written against the instance

The first pass redacted exactly the shape `log_function_call` emits, because
that was the line in front of it, and shipped. **Enumerating the writers into
`django_actions.log` — the question that should have come first, and the same
question the rule above is about — found two more, and both are worse:**

```python
# src/view/kai_user_dashboard.py, request_closure and request_drop_case
logger.info(f"{user.username} requested closure for Kai report '{report.title}' (ID: {report.id})")
logger.info(f"{user.username} requested to drop Kai report '{report.title}' (ID: {report.id})")
```

Both views are `@login_required` and **party-facing** — the caller is the
submitter or the accused — so each line carries a party's identity *and* the
case title. A third site, `kai_reports.py`'s
`[KAI EMAIL] Email queued for report: {report.title}`, leaked the title alone.

*Eight releases of this project have recorded the pattern "a rule stated
correctly, then something left outside the helper". This is the ninth, and the
thing left outside was found by asking the question the rule itself names.*

### ⚠️ And a fourth writer, found only by reading the log files

Asked to rotate the logs, I read them first. In them:

```
[SUCCESS] | User: System (unknown) | Action: CREATE | Resource: KaiReport
| ID: 1 | Details: {"model": "KaiReport", "instance_id": "1",
"title": "Case with a recused seat"}
```

`src/models/activity.py` registers `log_model_save` and `log_model_delete` on
`post_save`/`post_delete` **with no sender**, so they fire for every non-Django
model in the project and attach `instance.title` when one exists. `KaiReport`
has one. **Every save of a Kai case wrote its title into the file
`/officers/system-logs/` renders**, and had done since the receivers were
written.

> **Read the artefact, not just the producers.** Three of the four writers were
> found by grepping code. This one was invisible to every grep I ran — it names
> no Kai symbol, lives in no Kai module, and interpolates a generic `title`
> attribute — and it took ten seconds to find in the output. **When a fix has an
> artefact, look at the artefact.**

Two consequences for the render-time rules, both of which were wrong until the
log was read:

* **`_KAI_MENTION` was `\bkai\b`.** The trailing word boundary excludes
  `KaiReport`, which is the exact token this writer emits — so the broadest
  producer of Kai content generated lines the redactor ignored. Now anchored at
  the start only (`\bkai`), which also catches `kai_reports` and
  `kai/submit-report/`. It will over-match a member whose username begins
  "kai"; that fails safe.
* **The quote rule only knew single quotes.** The `Details:` blob is JSON.
  Rather than redact every double-quoted run — which would turn
  `{"model": "KaiReport", "instance_id": "1"}` into a row of ellipses and take
  the operational content with it — `_JSON_CONFIDENTIAL` targets the *values of*
  `"title"`, `"name"` and `"description"`. `test_it_keeps_the_model_and_the_id_on_that_line`
  is the control.

Fixed at source too: `_model_is_confidential(sender)` — **derived from the module
path** (`models.kai`), so a Kai model added later is covered by whoever adds it —
suppresses the title/name detail while leaving the model name and id, which name
nobody.

### ⚠️ It fails closed when the member list cannot be read

Scrubbing usernames needs a query, and `/officers/system-logs/` is the page you
open when things are broken — the same blast-radius concern v3.18.3 recorded
when a missing Kai migration could 500 the page you would use to diagnose it.
`member_usernames()` returns `None` rather than raising, and a Kai line whose
names cannot be checked is **withheld** rather than rendered unscrubbed. The
decorator shape is handled before any of that and needs no database, so the
common case still renders during an outage, and a non-Kai line is untouched.
Four tests, including both controls.

### The fix, in two halves

**At source — this is the actual fix.** All three sites now log
`("… Kai report %s", report.pk)`. An id names nobody.
`NoKaiViewLogsAConfidentialAttributeTests` is the general form: an AST walk over
`src/view/kai_reports.py` and `src/view/kai_user_dashboard.py` failing the build
if any logging call interpolates an attribute in `_CONFIDENTIAL_ATTRIBUTES`
(`title`, `description`, `username`, `name`, `submitted_by`, `targeted_to`, the
Kai note fields …). `pk`, `id` and `report_id` are deliberately absent from that
set. Its companion `test_the_detector_can_actually_fire` runs it against a
fixture of the exact code that was removed — v3.21.7's rule, applied on the way
in.

**At render — for the lines already on disk**, which nothing can unwrite.
`redact_kai_log_message` now removes three things:

* the actor of a `User X called <kai view>` line;
* any token equal to a member's username;
* any single-quoted run, which on a Kai line is case content.

⚠️ **The view-name set is DERIVED**, by walking `src/view/kai_reports.py`, plus
two named entries from `serve_private_upload.py`. v3.19.6's rule applied on the
way in: *a set is only the general form if something enumerates the population
it is drawn from.* A Kai view added next year is covered by whoever adds it.

⚠️ **TWO SHAPES, TWO RULES, AND THE DECORATOR SHAPE STOPS AT THE ACTOR.**
`log_function_call` writes `repr()` of the view's kwargs, and `repr()` of a dict
single-quotes its keys — so running the prose rules over that line redacts
`'report_id'` itself. Caught by `test_it_keeps_the_view_and_the_arguments`,
which is why that test asserts on the key and not just on the number. The two
triggers also have to be separate: `User X called submit_kai_report` contains no
standalone word "Kai" (the underscores swallow the word boundaries) and the
dashboard's prose line contains no view name, so either test alone misses half
the population.

⚠️ **Only identity and case content are removed.** The view name, the case id,
the arguments, the level and the logger all stay. An operations page emptied of
operational content is indistinguishable from a deleted one.
`test_the_control_line_still_names_its_actor` and
`test_a_quoted_string_on_a_non_kai_line_is_untouched` fail if the redaction
spreads.

⚠️ **`search`, not `match`.** The first draft anchored the pattern at the start
of the string, which made the viewer's raw-line fallback branch **a guard that
could not fire**.

⚠️ **Not per-viewer, deliberately** — a conditional redaction here would
reintroduce the shape of the v3.18.4 bug.

⚠️ **Named limit.** A display name, or a legacy numeric `user_id` (which for
members initiated before v3.23.0 *is* the roll number), is not removable by rule
from arbitrary prose. Nothing writes either any more, so this is a statement
about history — and rotating `logs/django_actions.log*` is the complete answer
for that.

**`src/test_kai_log_redaction.py`, 31 tests. Negative control: with the source
sites restored and the redactor neutralised, 7 fail**, including the AST walk
and both end-to-end reproductions; restoring the fourth writer and narrowing the
render rules back fails a further 4 across this file and the scrubber's.

### `manage.py scrub_action_logs` — for the files already on disk

New management command. It rewrites every line of `logs/django_actions.log*`
through the **same** `redact_kai_log_message` the log viewer uses, so the files
and the page agree.

⚠️ **Rotating would not have been enough, and would have looked like it was.**
`RotatingFileHandler` here keeps `backupCount = 3`, so a rotation renames the
file; the content stays on disk. It *would* clear the page, because the viewer
reads only the live file — which is exactly the kind of fix that looks complete
from the surface you were watching.

⚠️ **It truncates in place (`r+` then `truncate()`) rather than replacing the
file.** Daphne holds an open descriptor; writing a new file and renaming over it
leaves the app appending to an unlinked inode — invisible, and the scrubbed copy
would stop growing. `test_it_keeps_the_inode_so_a_running_process_keeps_writing`
pins it.

⚠️ **Dry run by default**, `--apply` to write, `--backup` for a `0600`
`.prescrub` copy on the first run — which **keeps the unredacted content and
should be deleted once you are satisfied**, since it is the thing the command
was run to get rid of, sitting in the directory it was removed from.

⚠️ **It refuses rather than half-scrubbing** if the member list cannot be read:
`WITHHELD` is the right answer for a page rendering now and the wrong one to
write permanently into a file. v3.25.1's rule — a tool that resolves one of the
two things it is asked to fix has moved the failure.

**New `src/test_scrub_action_logs.py`, 17 tests**, written with the command
rather than after it, because v3.25.1's entire finding was a tool that had none.
Run against this repo's own logs: 6 files, 1,945 lines, **9 redacted**, and a
second run reports 0.

---

## 🟠 2. A failing `subTest` aborts `--parallel` and reports nothing

Found by running the suite.

```
multiprocessing.pool.MaybeEncodingError: Error sending result:
  '(69, [('startTest', 0), ('addSubTest', 0, <unittest.case._SubTest ...
  Reason: 'AttributeError("Can't pickle local object
           'convert_exception_to_response.<locals>.inner'")'
```

**No `Ran N tests` line, no `FAIL:` line, and no results for the 1,600-odd tests
that passed.** This is v3.19.9's finding in the half v3.19.9 declared
unassertable, and `src/test_parallel_reporting.py` was green throughout.

> ⚠️ **A GUARD THAT PINS THE CONDITION A FIX TURNED ON IS NOT A GUARD ON THE
> FAILURE MODE.** That file pins `tblib is not None` — a *dependency*. The
> symptom returned through the same pipe by a different route and the guard
> could not see it. Companion question to v3.21.7's: not only *can this check go
> red*, but *would it notice if the original symptom came back?*

### ⚠️ The mechanism — established this time, not guessed

The first pass recorded it as unknown and said so. It is knowable, and it is
Django's:

```python
# django/test/testcases.py — SimpleTestCase.__getstate__
"""Make SimpleTestCase picklable for parallel tests using subtests."""
state = super().__dict__
if state["_outcome"]:
    ... return only the values that pass is_pickable() ...
return state                          # ← unfiltered
```

**The filtering is conditional on `_outcome`, and `_outcome` is cleared when the
test finishes.** `RemoteTestResult.addSubTest` calls `check_subtest_picklable`
*during* the test, where `_outcome` is truthy, the filter drops `self.client`,
and the check passes silently. The pool pickles `result.events` *after the
subsuite has finished*, where `_outcome` is `None`, `__getstate__` hands back the
raw `__dict__`, and `client.handler._middleware_chain` —
`convert_exception_to_response.<locals>.inner` — cannot be pickled.

Confirmed directly: the same `_SubTest` object pickles during the test and
raises that exact `AttributeError` once `_outcome` has been cleared.

> **A picklability check on a live object is a statement about that instant, not
> about the object** — and here the object's own `__getstate__` changes
> behaviour between the two instants, so the check is asked in the one window
> where the answer is favourable.

### ⚠️ Why every earlier minimal reproduction "did not reproduce"

`DiscoverRunner.build_suite` computes `processes = min(self.parallel,
len(subsuites))` and only builds a parallel suite `if processes > 1` — and
subsuites are partitioned **per TestCase class**. **`--parallel=8` over a single
test class runs serially, silently.** Every probe written to reproduce this, in
this batch and on the evidence of its write-up in v3.19.8, was a single class.
None of them was ever running in parallel. It is not batch dependence and it is
not flakiness, and the correction matters because "we could not reproduce it" is
what stopped three previous releases from finding the cause.

With two classes it reproduces in ten seconds.

### The fix

`_SubTestDescription`, `PicklableSubTestResult` and `PicklableSubTestRunner` in
`src/cache_isolated_runner.py`, wired to the existing
`CacheIsolatedParallelSuite`. Only `__str__` and `shortDescription()` are
needed: the parent replays the event into `TextTestResult`, which reaches the
subtest solely through `getDescription()`.

**`src/test_subtest_parallel_reporting.py`, 11 tests**, of which two matter
most:

* `TheRunnerReportsAFailingSubTestTests` runs a real `manage.py test
  --parallel=2` over a scratch package **with two TestCase classes** in a child
  interpreter and requires it to report `Ran 2 tests` / `FAILED (failures=2)`
  with the subtest descriptions intact. Against the pre-fix tree the child dies
  with `MaybeEncodingError` and prints no `Ran` line at all. This is the guard
  the previous release said could not be written.
* `TheDjangoAsymmetryThatCausesItTests` pins the upstream behaviour, so the day
  Django filters unconditionally this goes red and somebody deletes the
  workaround instead of inheriting it forever.

⚠️ **Substitution is on the failure path only** — Django records no event for a
passing subtest, and changing that would be a behaviour change dressed as a fix.

⚠️ **The asymmetry test builds its middleware chain with `load_middleware()`,
not `client.get('/')`.** The first draft made a real request from a
`SimpleTestCase`, which reaches `LockdownMiddleware` and queries a singleton —
so it passed only when an earlier test in the same process had warmed that
cache, and failed the moment it ran beside a sibling class. Caught by the full
suite, which is the point of running it.

**Negative control: 2 of 11 fail with one line of `CacheIsolatedParallelSuite`
commented out**, including the end-to-end one.

---

## 🟡 3. The production security configuration was executed by zero tests

`Parliament/settings.py` puts `SECURE_SSL_REDIRECT`, `SECURE_PROXY_SSL_HEADER`
and all three `SECURE_HSTS_*` inside `if not DEBUG:`, and derives `USE_HTTPS` —
hence `SESSION_COOKIE_SECURE` and `CSRF_COOKIE_SECURE` — from `not DEBUG`.
`.github/workflows/ci.yml` sets `DJANGO_DEBUG: 'True'`. **All seven values are
absent or `False` in every run this project has ever done, and nothing asserted
what they become in production.** The 2FA flags beside `DJANGO_DEBUG` in
`ci.yml` carry a written justification; `DJANGO_DEBUG` does not, and it is doing
far more.

⚠️ **Not fixed by flipping the flag.** Measured: at `DEBUG=False`,
`src.test_education_scoring_and_meetings` alone produces **59 failures and 44
errors**, because `SECURE_SSL_REDIRECT` turns every plain-HTTP test-client
request into a 301 and the suite is written against 200s. That is a project.

**New `src/test_production_security_settings.py`, 7 tests.** Django reads
settings once per process and `override_settings` cannot re-run a module-level
`if not DEBUG:` block, so the settings module is imported **in a child
interpreter** with `DJANGO_DEBUG=False` and the values are read back as JSON —
the same technique, for the same reason, as `test_parallel_reporting.py`'s
spawned-worker tests.

⚠️ **`settings.DEBUG` IS A LIAR INSIDE A TEST.** Django's
`setup_test_environment()` sets `settings.DEBUG = False` *after* the settings
module has been evaluated, so it reads `False` while the `if not DEBUG:` block
was skipped. The first draft of the control test asserted `settings.DEBUG is
True` and failed for exactly that reason. **The derived values are the evidence;
the flag is not.**

`TheseValuesAreNotOnInTheSuiteTests` records, as an executable fact, that the
configuration the other class checks is switched off in every run of this suite.
When the suite is eventually made `SECURE_SSL_REDIRECT`-safe and CI flips, that
class goes red and should be deleted. **Negative control:** weakening
`SECURE_HSTS_PRELOAD` and `USE_HTTPS` in `settings.py` fails 2 of 7.

---

## 🟡 4. The log page had a wider audience than the pages it records

`view_logs` was `@officer_or_advisor_required`, which admits advisors.
`review_excuses` — the page `serve_excuse_document` serves, and whose help text
says "doctor note, etc." — is `@officer_required` and deliberately does not. So
an advisor could not open a member's excuse document but could read
`User <member> called serve_excuse_document with arguments: (), {'excuse_id': 7}`
in the log tail, learning that a named member has a medical excuse on file.

> **A raw application log inherits the audience of its narrowest line, not its
> widest reader.**

Narrowed to `@officer_required`. This is a judgement call rather than a defect
report: advisors are read-only outsiders and a Django log tail is an operations
tool. **It is a one-word change to put them back** if that is wrong, and the
reasoning is in a comment at the decorator. Chosen over adding a redaction rule
per subject area, which is how a redaction becomes a guard written against the
instance — the mistake this same release already made once.

Two tests: an advisor gets 403, and a **chair still gets 200** (the control —
`officer_required` must not have quietly become admin-only).

---

## 🟡 5. `@log_function_call` turned `ParliamentUserAdmin` into a function

```python
@log_function_call                              # ← removed
@admin.register(ParliamentUser, site=admin_site)
class ParliamentUserAdmin(admin.ModelAdmin):
```

The wrapper is `def wrapper(request, *args, **kwargs)` and its first act is
`request.user.username`, so the module-level name became a *function* and
constructing it raised `AttributeError: type object 'ParliamentUser' has no
attribute 'user'`. Third instance of a category this project knows —
`_get_kai_access` carried three orphaned request-shaped decorators until
v3.16.2.

⚠️ **Why it survived, and why it is only 🟡.** `@admin.register(...)` is the
*inner* decorator, so it ran first and registered the real class; `/admin/`
worked perfectly. `functools.wraps` then copied the class's `__dict__` onto the
wrapper, so `hasattr(ParliamentUserAdmin, 'login_as_link')` was `True` and
v3.25.0's own `TheAdminCannotMoveAPrimaryKeyTests`, which reaches for exactly
that name, passed straight through it. A landmine, not a live bug.

> **A decorator written for views takes `request` first, so putting one on
> anything that is not a view is a type error Python will not raise until the
> wrapped thing is called — and for a class registered by an inner decorator,
> that call may never come.**

**New `src/test_admin_classes_are_classes.py`, 3 tests**: the reproduction, a
walk over `admin_site._registry`, and an AST check that no `class` in the admin
modules carries a decorator outside `{register, admin.register, receiver}`.
**Negative control: all 3 fail** with the decorator restored.

---

## Reviewed and found sound — do not re-audit

* **`Event.prime_attendance_stats` and the memoisation** (v3.25.0). The
  `.order_by()` argument, the `'late'` handling and the deliberately-unnarrowed
  filter are all correct.
* **The budget fixture builds 4 upcoming events as well as 24 past ones** —
  v3.25.0 learned its own lesson inside the same release. And `upcoming_events`,
  which the template also renders twice and the view does **not** cap, costs one
  query for both loops (a queryset caches `_result_cache`) and touches only
  `select_related('service_event')` per row.
* **The three member-search fixes.** Re-grepped: `user_list.py` was already
  correct and there is **no fourth** `user_id__icontains` member search.
* **`scripts/stamp_ledger.py`'s row insertion** (v3.25.1) — exercised for real
  during this review: v3.25.1 was committed as `7296056` and `bb8c22c` stamped a
  row the pre-fix script could not have created.
* **The remaining `@log_function_call` sites on slating and service-hours file
  views.** They log the *reviewer*, not the subject, and neither slating promise
  (ballot anonymity, notes destruction) is touched. Narrowing the page's
  audience (§4) is the answer to that class, not more redaction rules.

## The deployment ledger

Mason confirmed 08-24-26 that **the only undeployed release is v3.25.2**. Eight
rows (v3.21.5 → v3.25.1) moved off *not deployed*.

* **The commit hashes are observed.** All eight re-derived with
  `git log --diff-filter=A` and matched both the existing Commit cell and each
  release's own `**Committed & pushed:**` line. Nothing needed correcting.
* **The dates are derived**, from the commit that added each changelog, and
  carry a **‡** saying so — the same convention and the same reason as the †
  block from 08-17-26. Mason confirmed the *set*, not per-release dates, and was
  not asked to reconstruct them from memory. Deployed is `≤ the date shown`;
  Commit is exact, and the ledger now says which is which.

`manage.py check` is clean, `src.W003` is silent, and
`TheRealLedgerIsStampedTests` reports nothing left for `stamp_ledger.py` to do.

## Gates

✅ **1,723 tests, 0 failures, 8 skipped** (3 by design, 5 needing
PostgreSQL/Redis), under two orderings — `--parallel=4`, and `--parallel=4
--shuffle` (seed 3119520471) · **bandit 0 MEDIUM / 0 HIGH** under the CI
invocation · `makemigrations --check` clean · `manage.py check` **clean, 0
issues**.

⚠️ Negative controls run for every finding: **7 of 31** (+ **4** more for the
fourth writer and the scrubber), **2 of 11**, **2 of 7**, **3 of 3** — plus the
subTest fix verified against the live reproduction rather than a fixture.

⚠️ **One of those controls caught a real mistake mid-session.** A stray `cp`
copied the repository's unpatched `activity.py` over the working one, silently
reverting the fourth-writer fix in both places. The full suite went red on
exactly the three tests written for it. *A fix you cannot lose is a fix with a
test.*

## Deploy

Code only — no migration, no dependency, no seeder, no static file. `git pull`
then restart `parliament-gunicorn.service`.

⚠️ **Then scrub the logs**, because the disclosure has already happened and the
code change only stops new lines:

```
python manage.py scrub_action_logs                    # see what it would do
python manage.py scrub_action_logs --apply --backup   # rewrite, keeping a copy
rm logs/django_actions.log*.prescrub                  # once you are satisfied
```

**Named limit:** a display name, or a legacy numeric `user_id` (which for
members initiated before v3.23.0 *is* the roll number), is not removable by rule
from arbitrary prose. The scrubber removes usernames, quoted case content and
the confidential JSON keys; anything else in old lines needs the file discarding
rather than rewriting.

⚠️ **Advisors lose `/officers/system-logs/`.** Deliberate; see §4. One word to
revert.
